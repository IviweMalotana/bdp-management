# BDP Admin (Vite) — build-time env. Vite inlines VITE_* at build, so set this
# in the environment where `deploy.sh` builds BDP.Web (dev machine / CI), e.g.
# create BDP.Web/.env.production with:

VITE_API_URL=https://api.bedifferentpackaging.com
